# 本番サイト URL（sitemap / OGP / Schema.org に使用）
NEXT_PUBLIC_SITE_URL=https://example.com

# サイト名
NEXT_PUBLIC_SITE_NAME=Media Pick

# 商品 JSON の読み込みパス（省略時: public/data → ../backend/data/processed の順で探索）
# PRODUCTS_DATA_DIR=/path/to/processed
